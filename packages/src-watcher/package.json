{
  "name": "@hackello/src-watcher",
  "version": "0.2.5",
  "description": "Transform and clear require cache when code changes.",
  "main": "lib/index.js",
  "repository": "https://github.com/hackello/npm-packages/tree/master/packages/src-watcher",
  "author": "Hackello <team@hackello.com>",
  "license": "MIT",
  "dependencies": {
    "@babel/core": "^7.0.0-beta.42",
    "@hackello/fs": "^0.1.3",
    "@hackello/log": "^0.1.2",
    "chokidar": "^2.0.3",
    "lodash.debounce": "^4.0.8",
    "lodash.isarray": "^4.0.0",
    "minimatch": "^3.0.4"
  },
  "files": ["lib"],
  "scripts": {
    "clean": "rimraf lib",
    "build": "babel src -d lib --ignore '**/__tests__'",
    "lint": "standard --fix",
    "test": "jest --passWithNoTests",
    "posttest": "npm run lint -s",
    "ci": "jest --passWithNoTests --coverage",
    "preci": "standard"
  },
  "jest": {
    "testEnvironment": "node",
    "clearMocks": true,
    "collectCoverageFrom": ["src/**/*.js"]
  },
  "babel": {
    "presets": [
      [
        "@babel/preset-env",
        {"targets": {"node": "6"}, "shippedProposals": true}
      ]
    ]
  },
  "standard": {"env": {"jest": true}}
}
